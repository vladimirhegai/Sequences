{
  "extends": "./tsconfig.base.json",
  "include": [
    "packages/*/src/**/*.ts",
    "packages/*/test/**/*.ts",
    "apps/*/src/**/*.ts",
    "apps/*/test/**/*.ts"
  ],
  "comment": "apps/slack (the hackathon app) is excluded — it has its own tsconfig and uses tsx, so its churn never breaks the frozen engine typecheck.",
  "exclude": ["node_modules", "apps/slack"]
}
