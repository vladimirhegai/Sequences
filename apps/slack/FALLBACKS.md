# Fallback documentation moved

The fallback contract, prep-mode switch, recoverable-paperwork catalog, and
failure-diagnosis runbook are now consolidated in
[SENTINEL.md](SENTINEL.md#fallback-contract).

This compatibility pointer remains so historical links do not break. Do not add
new fallback guidance here; update SENTINEL.md and the executable registries.
