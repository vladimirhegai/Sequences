# DOC_AUDIT_2026-07-10 — stale-reference audit (conservative pass)

Scope: `apps/slack/*.md` (except PROBE_LOG.md, PROBE_FEEDBACK.md,
MOTION_QUALITY_PLAN.md, AGENTS.md), `apps/slack/docs/history/`, root
`CLAUDE.md`, `docs/paused/README.md`. Docs-only; no rule/policy changes; no
source-comment fixes. This report seeds the next session's full docs
restructure.

## Fixed this pass

### Broken relative links (mechanical — files moved to `docs/history/` after these docs were written)

- `docs/history/HANDOFF.md` — 9 links repointed: `ROADMAP.md`/`SENTINEL.md`/
  `FALLBACKS.md`/`CLAUDE.md` → `../../…`; `../../docs/RECIPE_STUDIO_PLAN.md`
  and `../../docs/RECIPE_STUDIO_HANDOFF.md` → `../../../../docs/…` (the real
  monorepo-root `docs/`). Same-folder links (SENTINEL_PLAN/REPORT,
  RECIPE_STUDIO_REPORT*, MOTION_DESIGN_PLAN) already resolved and were left.
- `docs/history/HANDOFF_FRONTIER.md` — 3 links: `PROBE_LOG.md` (×2),
  `ASSETS.md` → `../../…`.
- `docs/history/MOTION_DESIGN_PLAN.md:17` — `SENTINEL.md` → `../../SENTINEL.md`.
- `docs/history/SENTINEL_PLAN.md:6` — `SENTINEL.md` → `../../SENTINEL.md`.

### Stale references to retired/moved docs (repointed or annotated as historical)

- `ROADMAP.md:394` — "the unbuilt level-2 follow-up now lives in
  `PLAN_camera_depth_level2.md`": file deleted (level 2 shipped 2026-07-04 in
  commit `6f63df5`); rewritten to say it shipped and the plan doc was retired.
  (The other mention, ROADMAP.md:789, already said "built, plan retired".)
- `ROADMAP.md:1160-1163` — present-tense "both docs carry a STATUS block"
  about WS_Improvements.md / LESS_FALLBACKS.md (deleted in `3d1e7df`); now
  past tense with a pointer to the "Full audit" retirement note
  (ROADMAP.md:1295-1298 is the authoritative retirement record).
- `ROADMAP.md:1785-1787` — bare "SENTINEL_REPORT.md" → linked
  `docs/history/SENTINEL_REPORT.md`.
- `ROADMAP.md:1791-1793` — the MOTION_DESIGN_PLAN MD1–MD6 section now opens
  with the archive pointer `docs/history/MOTION_DESIGN_PLAN.md`.
- `ROADMAP.md:1844-1846` — the 2026-07-07 "Session 2 — canvas builder +
  agents" entry (cites `studio/canvasModel.ts`, `compileCanvas.ts`,
  `studio/agents/`, `npm run studio:canvas` — all removed) is annotated
  *REMOVED in the 2026-07-10 studio pivot*; the dated record itself is kept.
- `ROADMAP.md:1950-1951` — bare "handoff in SENTINEL_REPORT" → linked
  `docs/history/SENTINEL_REPORT.md`.
- `ROADMAP.md:2078-2080` — `docs/PROBE_AUDIT_HANDOFF.md`: **unresolvable** —
  never committed to git (working scratch file, since deleted; no archive
  copy exists anywhere). Annotated as such; the section is the surviving
  record.
- `SENTINEL.md:281-282` — "see SENTINEL_PLAN §3 Phase 3.2" → linked
  `docs/history/SENTINEL_PLAN.md`.
- `CLAUDE.md:495` — "`vendor/font-pairing/` sandbox" annotated "since deleted
  from the tree" (directory no longer exists; `typeSystems.ts` is the
  successor).

## Verified accurate — no change needed

- `apps/slack/README.md` — all 10 table links resolve; the
  Current/Historical framing matches the tree.
- Root `CLAUDE.md` and `docs/paused/README.md` — every link target exists
  (docs/paused/{WORKSPACE,FORGE,SEQUENCES,PLAN,LINEAR_DESIGN,MOTION_RESEARCH,
  MOTION_CATEGORIES}.md, `scripts/publish-public.sh`, apps/slack docs).
- `apps/slack/CLAUDE.md` — all ~45 cited source files, templates, tests, and
  npm scripts verified present (incl. `continuityGraph.ts`,
  `cameraBlocking.ts`, `browserLifecycle.ts`, `recipes/README.md`,
  `studio/INTEGRATION.md`, `continuity:replay`/`browsers:clean`/`studio:golden`
  scripts). The motion-polish-invariants and studio sections reference only
  existing files. Only defect found was the font-pairing note above.
- `ROADMAP.md` task-3 check: a 2026-07-10 Continuity Graph + Camera Blocking
  pointer already exists at the top — feature-map row (ROADMAP.md:30) plus the
  "Built Major Motion Feature" section ("Implemented and stress-tested
  2026-07-10", ROADMAP.md:111-129). No line added.
- ROADMAP links to `docs/history/SENTINEL_PLAN.md`/`SENTINEL_REPORT.md`
  (1481-1482), `../../docs/RECIPE_STUDIO_PLAN.md` (1838),
  `../../docs/RECIPE_STUDIO_HANDOFF.md` (1859), and
  `vendor/hyperframes/UPSTREAM.md` (1592) all resolve.
- "Files That Define the System" table (ROADMAP.md:1561+) — every row's file
  exists.

## Deliberately left for the big restructure (do NOT treat as overlooked)

1. **CLAUDE.md is 1,019 lines and half of it is a dated changelog.** The
   "Current feature state" section spans CLAUDE.md:482-924 (~440 lines) and
   accretes 2026-07-01…07-09 session narratives (WS1…WS7, codex-audit,
   fallback-elimination) that duplicate ROADMAP's dated sections. Candidate:
   keep the rules/boundaries sections (1-481, 925-1019), move the narrative
   into ROADMAP or docs/history.
2. **ROADMAP.md is 2,328 lines mixing live state with history.** Dated
   session logs start at ROADMAP.md:1781 (`## 2026-07-06 — Sentinel
   completion`) but WS/audit history is also interleaved into "What is Built"
   (e.g. 1079-1400). The "Built Major Motion Feature" section (66-129) still
   reads as a build plan ("Build a persistent semantic scene graph…",
   "Suggested owners: a new `src/engine/continuityGraph.ts`" at ~93) with the
   shipped notice appended at 111 — should be rewritten as shipped-state.
3. **FALLBACKS.md / SENTINEL.md overlap.** Both own parts of the same story:
   the prep-mode flag lives at FALLBACKS.md:22-45 AND in SENTINEL.md's flag
   table (SENTINEL.md:474); attempt-ladder/degrade policy appears in
   SENTINEL.md:270-282 and again in FALLBACKS' class catalog. One should own
   flags, the other classes, with cross-links.
4. **Retired-doc prose mentions kept as history.** IMPROVEMENT_PLAN /
   WS_Improvements / LESS_FALLBACKS are mentioned in prose throughout dated
   ROADMAP sections (1065, 1082, 1198, 1259, 1342, 1380, 1387…); retirement
   is documented once at ROADMAP.md:1295-1298. Left as-is — they are inside
   dated history sections.
5. **docs/history/HANDOFF.md describes the canvas builder/agent chat as
   shipped** (lines 29-31, 47-56) — accurate as of 2026-07-07, superseded by
   the 2026-07-10 pivot. Historical record; left unedited beyond link fixes.
6. **MOTION_QUALITY_PLAN.md:340** notes `SENTINEL_PLAN.md` is cited ~10× in
   source comments — source files were off-limits this pass; those citations
   still point at the pre-move path.
7. **ARCHITECTURE.md** is target-design (its own header says use ROADMAP for
   shipped reality); its speculative references (`frame-presets/`,
   registry-item.json, model ids) were not audited against the tree.

## Unresolved / skipped

- `docs/PROBE_AUDIT_HANDOFF.md` (ROADMAP.md:2078) — no surviving copy in git
  history; annotated, cannot repoint.
- PROBE_LOG.md was concurrently modified by another agent during this pass;
  it was off-limits anyway and was not touched.
