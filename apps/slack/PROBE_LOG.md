# Live Probe Ledger

## Policy

When a paid probe burns an attempt or falls back, inspect its run artifacts.
For any non-architectural cause, fix it deterministically, add a regression
test, record the result here, then resume the original work. Follow the
placement tree in [SENTINEL.md](SENTINEL.md); do not loosen a gate or compensate
with prompt prose. Park architectural changes in [ROADMAP.md](ROADMAP.md).

Older incident detail remains in ROADMAP audit sections. This ledger is the
current source of truth for probe outcomes from 2026-07-09 onward.

| Date | Probe / Job | Stage | Burned | Root Cause | Fix And Evidence | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-11 | `architecture-stress-2-20260711` (`PulseForge`, stress 1) | storyboard-plan | 1 findings-retry; run stopped before attempt 2 | The slow-motion approval scene introduced its third surface 2.0 viewer-seconds before the cut, versus the 2.7s development floor. `stretchMarginalPacingMisses` already owns bounded introduction deficits, but a stale blanket exemption skipped every resolvable time-ramp scene even though ramp boundaries are net-zero identity points. | The stretch pass now measures introduction, reading, and outcome deficits in viewer time for ramped and ordinary scenes. A bounded cut extension remains exact after a ramp and still commits atomically. The rejected raw response now passes strict replay at 25.203s with a 0.703s host stretch; all 79 pacing regressions pass. The director guide also distinguishes destination overlap from source-station beats, closing the genuine departure collision found in the LaunchRelay opener. | Fixed from exact attempt; rerun required |
| 2026-07-11 | `architecture-audit-live-1-20260711` (`LaunchRelay`, audit stress baseline) | storyboard-plan / source-author / browser + temporal QA / render | 3 storyboard attempts; 3 source attempts; 13 logical / 17 physical calls; 35.9m; `published-degraded`; least-bad 58; rendered 27.3s MP4 | Exact artifacts exposed six common-layer defects: neutral continuity chassis was lost by unrelated atomic rollback; plugin children lost their declared station on reparse; a host-auto camera moment protected and then outlived an optional same-station pull-back; global `gsap.* + delay` slot motion escaped the seekable `tl`; a full move into a shared hero/sidebar station promoted both children to primary and stole the hero dwell; and a dark-basis `screen-over-wallpaper` environment still painted a near-white pedestal, visually overwhelming the intended hazard-dark film. The baseline strip also showed a 2.44s dead final hold, 7 jerk clusters, only 8/9 readable primary landings, and visible `check`/toast sizing residue. | Chassis/rack-focus monotonic repairs now survive rollback; plugin lowering/restamping carries `region`; same-station dropping requires one proven content station and host-auto moments regenerate from surviving evidence; slot normalization binds global tweens to `tl` with explicit positions and the prompt names the correct call form; shared-region support remains supporting unless the destination is unambiguous; dark pedestals honor the committed basis. Repo-relative replay paths and strict storyboard replay were repaired for exact-probe diagnosis. Focused 402-unit pass + typecheck are green; exact attempt-2 replay now leaves only its two genuine opening-chaos pacing conflicts. | Baseline failures fixed at common layers; stress verification required |
| 2026-07-09 | asset-probe-1 | storyboard-plan | 1 findings-retry | `cameraArrivalSec` read a same-station re-frame as a late arrival and anchored an asset entrance inside a required hold. | `pluginContract.ts` now honors the camera entry frame; camera-arrival regressions in `pluginContract.test.ts`. | Fixed |
| 2026-07-09 | asset-probe-1 | source-author | 3 environmental attempts | The probe defaulted to `claude-code-cli` and hit its session limit. | Probes must pass `--provider openrouter-api`; no product defect. | Environmental |
| 2026-07-09 | asset-probe-2 | source-author | 1 compact repair | Five author-authored GSAP tweens targeted selectors absent from the final markup; GSAP warned but performed no work. A dropped supporting moment was already non-blocking. | L2 `stripDeadGsapTweens` removes only standalone literal-selector no-ops after host injection; regression in `authorReliability.test.ts`. Published clean: 16/16 moments, 10 thumbnails, critic applied, no fallback. | Fixed; assets default ON |
| 2026-07-09 | polish-audit-20260709-a | source-author / critic | 2 browser rejects; 1 critic timeout | Slot 1 called bare `fromTo(...)`; slot 2 invoked `window.__tl_scene_*` pseudo timelines that the host never creates. The third source published, but its technically passing film exposed sparse framing, evidence bound to scene starts instead of named actions, a stretched live-DOM morph, a hidden count target, and a part-targeted final camera shot that cropped the injected lockup. | `normalizeSceneSlotScript` canonicalizes both model envelopes before assembly; `componentMorph.browser.test.ts` proves the material bridge; camera, moment-binding, sparse-framing, morph-focal, hidden-stat, and load-bearing-companion regressions cover the visual failures. The paid run still produced a 19s MP4 with no fallback, 10 bound moments, 12 component beats, and 3 full camera moves. | Fixed from artifacts |
| 2026-07-09 | polish-audit-20260709-a exact-source replay r6 | deterministic replay / browser QA | 0 model attempts | Replaying the same paid source isolates engine gains from model variance. The original contact sheet contained a tiny cold open, mid-morph/black review frames, a cropped chart, and a final ring shot missing its headline/CTA. | Replay now content-frames the cold open, captures the intended component/camera evidence with 2.5% review-safe margins, keeps the full chart and plugin lockup in frame, animates the visible stat root, and reports no focal/sparse/safe-area failures. Only one supporting `moment_static_frame` advisory remains for a small 0-to-7 glyph change. Evidence: `.data/audit-20260709/orbitops-replay-contact-sheet.png`. | Fixed; visual residual documented |
| 2026-07-09 | direction-live-a-20260709 | storyboard-plan / source-author | 2 storyboard findings-retries; 1 source repair; 18.3m total | The first storyboard's one pull-back crossed two typed-copy holds and a late metric swap; the second left a three-second push-in moving through a set-state payoff. The source then used an indexed `forEach` stagger the static liveness parser could not place, while primary-focal QA incorrectly checked the scene's later metric during an activity-feed moment. The camera also opened that scene on the later scene focal even though its immediate first segment established the feed station. | `delayConflictingCameraMoves` now clears in-flight holds, preserves only semantically camera-bound moments, and drops a non-load-bearing reframe when multiple holds leave no clean slot. Exact rejected-attempt replay now accepts storyboard attempts 1 and 2. `motionDensity` resolves deterministic inline index staggers; primary focal review follows component/interaction evidence; immediate first camera segments establish their own target. Re-injecting the exact published source now yields no motion warning, no focal issue, `strictOk:true`, and zero browser warnings. Regressions: `pacingAudit`, `motionDensity`, `layoutInspector`, and `cameraContract`. The paid film itself published without fallback (9/9 moments, 3 full moves, 9 thumbnails). | Fixed from exact artifacts |
| 2026-07-09 | direction-live-b-20260709 (first launch) | process / storyboard-plan | 1 interrupted run; 20m outer timeout | The provider-bound run outlived the original command timeout and exited later without a storyboard, composition, failure artifact, or stage receipt. The incomplete directory was preserved as `.data/projects/direction-live-b-timeout-20260709`; there is no deterministic product finding to repair from it. | Re-ran the same brief under a 30-minute command bound. This was command/provider latency rather than a validator or runtime defect. | Environmental |
| 2026-07-09 | direction-live-b-20260709 | source-author | 2 browser rejects; 19.7m total | Each rejected document put otherwise-valid slot motion inside an uninvoked `(tl) => {...}` envelope, so all authored `tl.*` calls were no-ops. Exact attempt-2 replay also exposed broad text occlusion, overlap, contrast, safe-area, and stale-surface problems after motion was restored; those require authored layout redesign rather than a safe local rewrite. | `normalizeSceneSlotScript` now unwraps only a complete redundant arrow envelope, including `const animate = (tl) => {...};`, with telemetry and regressions in `sceneSlots.test.ts`. Exact attempt-2 replay changes six static slots into live ones and clears all `interaction_not_visible` and static-moment findings. The remaining layout class is parked in ROADMAP. The final paid re-author published without fallback: 13/13 moments, 18 component beats, 2 full camera moves, 10 thumbnails, no motion warning. | Deterministic no-op fixed; layout residual parked |
| 2026-07-09 | direction score A/B replays | deterministic playback comparison | 0 model attempts | Establish whether host direction improves coordination without forcing one rhythm onto every brief. | Northstar directed playback improved measured settles from 4/8 to 5/8 and reduced independent motion from max 4 / mean 1.23 to max 3 / mean 1.10; focal results were unchanged and jerk evidence was mixed. Ledgerly stayed 6/14 settles with max 3 independent voices and mean 0.361→0.356. The golden film had no automatic camera/FX consumer and remained unchanged. Continuous evidence stays advisory. | Keep direction default ON; no global thresholds yet |
| 2026-07-10 | session26-camera-probe-1 (Vectorline) | storyboard / source / temporal audit | 4 storyboard findings-retries; 19.5m total | The planner repeatedly omitted an explicitly requested rack focus despite supplying suitable moves; one source used malformed three-argument `fromTo`; evaluator attention preferred the scene focal over the active phrase/camera target; a final reframe landed on the cut; morph evidence sampled only the incoming selector. | Conservative rack-focus top-up, GSAP call-shape repair, phrase/active-camera attention precedence, cut-straddling morph evidence, and a 0.42s final landing reserve were added with focused regressions. The accepted 31.8s diagnostic had 5 scenes, 21 moments, 14 beats, 9 full moves, and no fallback. | Root causes fixed; diagnostic only |
| 2026-07-10 | session26-camera-probe-2 (FlowPilot) | storyboard-plan | 3 findings attempts; 22.0m; fail-loud | A findings retry added the requested metric development as a scene fully embedded in its parent window. Generic contiguous-scene rebasing moved it later and recreated a moment gap. | `mergeEmbeddedDevelopmentScenes` folds only exact duplicate-surface, contained development patches into the parent. Any new creative surface or escaped cue remains blocking. Exact rejected response replays clean. | Fixed; failure not publishable |
| 2026-07-10 | session26-camera-probe-3 (FlowPilot) | render / status reporting | render timed out after ~16.5m; 27.1m total | Both direct render paths hard-forced SwiftShader (`browserGpuMode:"software"`) and screenshot capture. Calibration reached 1712ms/frame with one worker and timed out; the CLI then labeled a requested render with no MP4 only `warn`. | Producer compatibility now owns capture with GPU mode `auto` and no forced screenshot. Exact same project calibrated at 124ms/frame, used 4 workers, captured in 29.8s, encoded in 6.2s, and finished in 41.3s. A requested missing/empty MP4 is now `fail`. | Core render defect fixed; failed run not publishable |
| 2026-07-10 | session26-camera-probe-4 (RelayGrid) | source-author / browser QA | 2 rejected sources; least-bad publish; 7.46m | Later authored CSS changed `.scene` to `position:relative`, collapsing all shots into 116px rows at y=580. Camera framing, CTA clipping, and cursor/ripple geometry then failed as misleading independent symptoms. Slot scripts also used host-timeline aliases, data attributes, `time` variables, and local scene clocks in mechanical forms. | The host stage now locks only structural root/scene/clip/overlay geometry with browser proof against later CSS. Slot normalization binds the certain timeline/root/position forms to the absolute film clock. | Fixed at common chassis; degraded artifact not judge-ready |
| 2026-07-10 | session26-camera-probe-5 (SignalPath) | storyboard-plan | 3 attempts; 3.4m; fail-loud | Atomic plan normalization rolled back a valid required rack-focus modifier when an unrelated timing repair minted a new finding class, so every retry re-earned the same missing-focus finding. | After an atomic arithmetic rollback, the monotonic required-focus modifier is reapplied to the reverted model plan and the gate is rerun. Exact rollback regression passes. | Fixed; failure not publishable |
| 2026-07-10 | session26-camera-probe-6 (SignalPath) | source-author / browser QA | 3 source attempts; 5.8m; fail-loud | Sources contained an uninvoked `(tl,root)=>{}` slot, decorative `d="... C..."` SVG geometry, and bare `borderColor:var(--positive)` JavaScript. Once those were replayed clean, the storyboard still withheld its first subject until 2.8s and correctly failed `near_blank_film`. | Complete two-argument slot envelopes bind to the host root; executable-script CSS vars are quoted; only unbound decorative ellipsis paths are removed. A hard storyboard gate now establishes the first declared subject within 1.25s, avoiding a paid author/browser discovery. | Root causes fixed; failure not publishable |
| 2026-07-10 | session26-camera-probe-7 (SignalPath) | full create / render / temporal replay | 2 rejected author drafts; critic repair; 6.3m total | The final create passed and rendered, but temporal review found that pacing retimes had mutated camera cue times without restoring array order. A 2s parallax segment was squeezed into 0.3s and connective drift overlapped an orbit, creating the dominant jerk cluster. The imagery also remained too sparse/tiny in several shots despite technical compliance. | `normalizeConnectiveCameraSchedule` makes drift/hold yield to full moves and sorts every post-retime path. Model-free revision 2 re-rendered in 38.0s; reversals improved 3→1 and settles 7/12→8/12. Final 28.8s artifact: 4 developed scenes, 19 moments, 8 beats, 8 full moves, no fallback, non-empty MP4. Remaining art-direction limitations are assigned to the continuity-graph roadmap feature, not mislabeled as solved. | Pass after exact-project correction; quality residual documented |
| 2026-07-10 | Continuity Graph exact A/B (`session26-camera-probe-7` → `continuity-ab-signalpath-1` r13) | deterministic replay / temporal QA / render | 0 model attempts | The independent-shot baseline reset framing, re-ran same-target zooms, and honored late supporting/orbit camera instructions. | Stable cross-shot entity ids, measured shared-element handoffs, primary-owned minimum-jerk routes, same-station holds, and connective-time anticipation. Exact replay improved mean visibility 61.9%→91.0%, off-frame samples 118→21, peak speed 1.682→0.470, acceleration 6.002→2.227, and jerk 43.015→22.562 without lowering settles (8/12) or reversals (1). All 11/11 primary landings are readable; corrected MP4 rendered in 39.3s. | Acceptance A/B passed |
| 2026-07-10 | `continuity-live-beaconops-20260710` | live create / exact replay / temporal QA / render | 2 source candidates reviewed; 9.0m live total | The first source put a host-generated lockup beneath the app window and browser QA reported occlusion/overlap; the alternate stagnated. Canonical continuity also initially selected multiple shell appearances, producing a self-edge. | Regionless lockups receive a safe-bottom host placement; continuity canonicalization chooses one scene representative (app-window priority), removes self-edges, and avoids shell→sidebar bridges. Exact r6 has a real three-shot product entity, 7/7 readable primary landings, 90.1% mean visibility, no fallback, and a 35s corrected render. | Attempt causes fixed; pass |
| 2026-07-10 | `continuity-live-ledgerflow-20260710` | live create / exact replay / temporal QA / render | 1 storyboard findings-retry; 2 source browser rejects; 11.0m live total | Non-camera moves were stretched across multiple payoff holds; the model's source retained legacy camera landings that clipped the exception station even though the graph runtime would own framing. | Multi-payoff non-camera moves now drop instead of stretching an empty tail; component moments anchor at beat completion; graph-owned layout QA audits measured primary blocking landings instead of obsolete authored camera segments. Exact rejected artifacts replay clean; r5 has two entities across 3+ shots, 8/8 readable primary landings, no fallback, and a 34s corrected render. | Attempt causes fixed; pass; art residual logged |
| 2026-07-10 | `continuity-live-threadline-20260710` | CLI / live create / exact replay / temporal QA / render | 1 pre-pipeline quoting failure; 1 storyboard findings-retry; 2 source browser rejects; 11.8m live total | Windows/npm split the spaced product argument before any model call. The storyboard left a 3.9s review gap during a long camera move; browser QA then evaluated inactive/off-camera text, legacy focal targets, and an unsafe CTA. A late supporting block compressed the final CTA travel to 1.942 diag/s. | Re-run used `ThreadlineAI`; long camera moves synthesize a midpoint review anchor; graph-owned QA ignores inactive scenes and evaluates every primary block; CTA framing is delivery-safe. Supporting blocks no longer drive the lens, and primary travel begins after the previous readable dwell. Final r4 is strict-clean apart from advisories, has 6/6 readable primary landings, zero reversals, speed 1.013 diag/s, 2 jerk markers (down from 8), no fallback, and a 31.9s corrected render. | Attempt causes fixed; pass; art residual logged |
| 2026-07-10 | `motion-quality-verify-1` (ParcelPilot re-brief) | source-author / browser QA | 2 browser rejects; least-bad penalty 4; 18.1m total | Storyboard passed in ONE attempt (the parcelpilot fixes held), but both source rejects carried `camera_blocking_landing` on `recovery-cta`: the audit judged the SUBJECT's solo occupancy range (measured 0.7% vs 1.8–14%) on an ensemble phrase whose declared `framingTarget` (`product-ui`, framingOccupancy 10–42%) makes the runtime deliberately cap zoom so the contextual station stays delivery-safe — a host-vs-host contradiction the author cannot repair (same family as parcelpilot's region-collapse bug, opposite direction). | `auditCameraBlockingLandings` and `buildCameraBlockingEvidence` now judge ensemble semantics: the framing station's painted-content union against `framingOccupancy` plus subject readability, while a framing station that collapses to the subject still binds the subject's own range (the ParcelPilot guard). Browser regression `cameraBlockingLanding.browser.test.ts` replays both geometries; evidence-waiver unit test in `cameraBlocking.test.ts`. The film itself rendered a 17.9s MP4 with no fallback. | Fixed; art residuals reviewed separately |
| 2026-07-10 | `motion-quality-verify-2-quillsign` | storyboard-plan / source-author | 2 storyboard findings-retries; 2 source rejects; least-bad penalty 7; 18.5m total | Three distinct host classes: (1) `deriveDiveWindows` ended its held window exactly at a covered set-state payoff, so the host-derived pull-back leg WAS the framing change `pacing/outcome` rejects — both storyboard retries re-earned the identical finding because the model does not own the legs, and the demoted advisory shipped as the film's degradation. (2) The component kit's `listSiblings` read `className.trim()` on an inline decorative `<svg>` sibling (SVGAnimatedString) and the whole compile threw before the timeline registered — one paid author attempt. (3) A target-less opening drift in the single-station ship-it scene read as a late 24.85s camera arrival, anchoring the host plugin lockup's entrance 0.85s before film end and stranding the declared 22.2s assemble moment without evidence — one more author attempt. | Dive windows now extend for a covered payoff's `OUTCOME_HOLD_SEC` before the pull-back (growing the dive when the scene has free time; `cameraDive.test.ts` proves `auditPacing` accepts the derived plan). The kit reads classes via a safe `classString` helper and skips foreign elements as selection peers (`navActive.browser.test.ts` compiles the SVG-sibling list). `cameraArrivalSec` treats a camera path naming no station other than the unit's own as on-frame from entry (`pluginContract.test.ts`). Storyboard cache contract v23. The film itself published with a rendered 25.7s MP4, 17/17 bound moments, no fallback. | Fixed; art residuals reviewed separately |
| 2026-07-10 | `motion-quality-verify-3-meridian` | storyboard-plan / source-author | 2 storyboard retries (1 truncation/compact-recovery, partly environmental — 5 physical call failures, 35m total on a slow provider night); 2 source rejects; least-bad penalty 38; shipped `cut-degraded` paperwork | TWO OPEN host classes for the next session (do these FIRST): (1) **morph-degrade loop** — declared morphs `chaos-open→triage-demo` and `one-click-resolve→payoff-cta` degraded at bind time ("outgoing focal part has no visible painted content" + aspect 7.59 toast vs 2.42 card silhouette mismatch); the author burned two compact repairs trying to restyle endpoints and never converged — this is exactly plan WS-D2 (fix top mechanical degrade causes host-side) and likely C3 (the toast had already exited/faded at the boundary). (2) **wrong blocking occupancy range on a plugin child** — `camera_blocking_landing` on `alert-stack-toast-1` at t=0.08s expected 12–48% occupancy for a TOAST (solo toast range is ~0.25–6%): `occupancyFor` appears to assign a hero/ensemble range to a lowered plugin child; also sampling at 0.08s of film start. Compiler-side, not author-fixable. Also shipped-advisory `pacing/outcome` on a NON-dive framing change (`shell-open` payoff, framing changes 0.3s later) — the dive fix does not cover cut/move conflicts that `delayConflictingCameraMoves` declines to retime; worth a look at its constraints. The dive/SVG/ensemble/arrival fixes from earlier today did NOT reappear. | OPEN — logged for fix-first at next session start; film rendered (`meridian-20260710-235245.mp4`), visible in the Studio Probes tab |
| 2026-07-10 | `motion-design-live-parcelpilot-20260710` | source-author | 4 attempts; 5.6m; fail-loud FAILURE.md | Two HOST-owned findings poisoned every attempt so author repairs could never converge: (1) `interaction_seek_instability` compared the cursor's absolute viewport coordinates across seek orders, charging a legitimate camera-world excursion to the interaction; (2) the blocking runtime solved zoom to the framing REGION's `framingOccupancy.preferred` (0.30) while the landing audit judged the SUBJECT against its own stat-card max (0.24) — when the region's only painted content was the subject itself, the host violated its own declared range (measured 29.9–30.0% across full re-authors). The remaining contrast/safe-area/rescue-syntax findings were ordinary author-repairable classes starved by the poisoned retries. | QA v21 compares the cursor-to-measured-anchor offset (regressions both ways in `layoutInspector.test.ts`); `blockingFrameState` detects a context rect that collapses to the subject and preserves the subject's own occupancy contract (`sequences-camera.v1.js`, comment cites this probe). Verification probe `motion-quality-verify-1` relaunched on the same brief shape. | Fixed; failure not publishable |
| 2026-07-11 | `motion-quality-final-lumaflow-20260711` / exact replay `motion-quality-final-lumaflow-replay-r2` | storyboard-plan / source-author / browser QA | 2 storyboard findings-retries; 2 additional rejected sources; least-bad penalty 62; 24.0m | Three host-owned classes amplified the run: (1) attempt 1's dive targeted `release-card` while the explicit click targeted sibling `approve-btn` in the same `focus-station`; attempt 2 merely retargeted the same lens, so this was deterministic action precedence, not a creative retry. (2) Blocking QA sampled 80ms after camera arrival, before host/component entrances had settled, charging fully readable `release-card`/`shipped-badge` landings to the author and reporting misleading occupancy-only messages. (3) the high-confidence dashboard recipe used demo product copy and duplicated the scene's authored app-window + `dashboard-grid`, producing the tiled/overlapped mess visible in the film. | Dive derivation now retargets only one unambiguous same-station interaction sibling; exact attempt-1 storyboard replay passes. Blocking audit/evidence reviews the bounded dwell's settled landing; focused unit/browser regressions cover delayed entrances. Auto-declared `product` params bind from the `Product:` brief, while the existing primary-dashboard governor absorbs the exact recipe/plugin collision. | Fixed from exact artifacts; verification probe pending |
| 2026-07-11 | `sequence-check-1783766368325` (`LumaFlowQC1`, live probe 1) | storyboard-plan / source-author / visual QA | 2 storyboard attempts; 4 source attempts including rescue; 12 logical / 16 physical model calls; 29.6m; `published-degraded` despite CLI `pass` | The first storyboard combined one app-window chassis with its same-station action bar, mislabeled a cursor arrival as camera-owned, and a scene repair put `rows` on that button. Source attempt 1 was otherwise valid but three interpolated `querySelector` templates hit the bundler lint; patch 2 fixed only one. The rescue render then exposed a second host/plugin theme gap: a light-basis frame did not seed semantic text/surface tokens, leaving the injected closing lockup white on white, while an anonymous four-metric row duplicated the typed dashboard-grid and clipped both stacks. The audit CLI also ignored Sentinel degradation, stage retries, and browser warnings. | Same-station app-window+child counts as one surface; interaction prose outranks a mislabeled camera intent; load-bearing button `rows` lowers to `set-state`. Inline selector templates lower to concatenation before lint, and exact attempt-1 source now passes static + browser QA without a model retry. Brand base now seeds the full semantic palette. A conservative 3+ anonymous-tile absorber removes the duplicate metric row without touching the declared risk card. Regionless lockups now center inside the pedestal with a delivery-scale CTA; exact browser replay removes the lockup anchor failure. `summarizeSequenceCheckStatus` now reports `warn` for any real Sentinel degradation/fallback, repeated stage, motion warning, or browser warning while preserving `fail` for missing required artifacts. Focused regressions plus exact artifact replays pass. | Attempt causes and visible host collisions fixed; verification probe required |
| 2026-07-11 | `sequence-check-1783769389952` (`LumaFlowQC2`, live probe 2) | storyboard-plan | 5 rejected storyboards; 7 logical / 10 physical model calls; 23.1m; fail-loud | Every attempt was charged 10–11 film-wide components against an eight-unit cap even though four CTA appearances declared the same stable `entityId:"cta"` and formed one continuous cross-scene object. The rescue eventually removed the short-scene excess but still failed the duplicated film-wide charge. | Film-wide component accounting now charges an explicit stable `entityId` once across scenes; per-scene density, unrelated surfaces, and same-scene duplicates remain fully charged. The exact rescue response now parses clean, while attempts 1–4 retain only their legitimate short-scene density finding. Focused 80-test pass and typecheck are green. | Fixed from exact artifacts; verification probe required |
| 2026-07-11 | `motion-quality-live-3-nodeharbor-20260711` (`NodeHarborQC3`, live probe 3) / exact source-attempt-1 replay | storyboard-plan / source-author / browser QA / visual QA | 1 storyboard stage attempt; 3 source attempts; 12 logical / 15 physical model calls; 20.3m; `published-degraded` least-bad penalty 30; CLI correctly reported `warn` | Five host contradictions made a viable first source look repairable: (1) one declared camera moment protected every overlapping move, so a redundant third full move could not be normalized; (2) the deterministic `const t=(s)=>sceneStart+s` helper made 6, then 14, then 32 valid cues look unpositioned; (3) an ensemble station fit was also judged against the subject's solo occupancy/anchor limits; (4) flow-diagram plugin edges were called stale for overlapping their own nodes; and (5) default-on environment scenes were told to duplicate host-owned ambient motion. A genuinely author-owned safe-area miss was repaired normally. | Camera-budget normalization protects only the closest eligible activity per declared moment. Scene-slot lowering inlines deterministic local-time helpers and the static audit resolves them in exact historical artifacts. Contextual framing owns ensemble occupancy/anchor semantics unless its union collapses to the subject. Plugin children are excluded from stale-surface checks, and environment-backed scenes retain quiet-window telemetry without paying a source retry for host motion. QA cache v36 invalidates pre-fix evidence. The exact original source attempt 1 now passes current static and browser QA with no issues or motion warning; all 5 primary landings are readable and all 15 occupancy samples are in range. Focused 140 unit tests, the landing browser regression, typecheck, and exact replays pass. | Fixed from exact artifacts; Probe 4 must verify one-attempt behavior |
| 2026-07-11 | `motion-quality-live-4-meterly-20260711` (`MeterlyQC4`, live probe 4) | source-author / browser QA / visual QA | 3 rejected sources; 10 logical / 12 physical calls; 12.5m; least-bad penalty 18 | Browser evidence mixed authored overlap with host timing and interaction ownership: a dense typed payoff was double-counted as authored motion, target/cursor positions were compared across camera-world coordinates, and pre-arrival samples were promoted into endpoint obligations. A declared morph also degraded after its outgoing endpoint had already disappeared. | Typed/host motion is excluded from authored-density pressure; cursor evidence stays target-relative and cursor phases no longer promote pre-arrival samples. Cut paperwork reports the executed fallback honestly. Exact attempt replays and focused interaction/camera regressions pass; the recurring invisible-outgoing morph class remains architectural WS-D2 rather than being hidden. | Attempt causes fixed; morph architecture remains explicit |
| 2026-07-11 | `motion-quality-live-5-routeboard-20260711` (`RouteBoardQC5`, live probe 5) / exact replay r7 | storyboard-plan / source-author / temporal QA | 2 storyboard attempts; 3 source attempts; 12 logical / 17 physical calls; 27.3m; least-bad penalty 27 | Persisted scene-slot arrows could remain uninvoked; focal entrance motion was misread as camera motion; cadence-adjacent jerk samples counted one gesture repeatedly; a late support destination moved the lens through the prior primary hold; and hidden quiet-window scoring penalized evidence the browser had waived. | Persisted arrows re-run the narrow slot normalizer; continuous evidence records camera-world speed separately and clusters jerk gestures; quiet-window ranking follows surfaced issues only. Explicit full-move destinations become camera-load-bearing, while primary dwells can no longer be borrowed for the next route. Exact replay keeps every timeline landing fully visible and at rest and the late publish action fully on-frame; focused 374-unit / 81-browser checks pass. | Fixed from exact artifact; no extra paid run needed |
| 2026-07-11 | `motion-quality-live-6-patchwork-20260711` (`PatchworkQC6`, live probe 6) / exact replay r7 | storyboard-plan / source-author / browser + temporal QA / render | 2 storyboard attempts; 3 source attempts; 11 logical / 14 physical calls; 18.4m; rendered 19s MP4; least-bad 85, critic 83 | The decisive host contradiction was architectural but local: continuity emitted 18 blocking phrases for five camera-less scenes, yet camera-world wrapping and `SequencesCamera.compile` were injected only when the planner authored a camera path. Every target therefore stayed tiny while two paid source repairs tried to resize markup. Wrapping then exposed ripple localization inside a scaled camera world. The first storyboard also used load-bearing `open` on an app-window, whose entrance is already host-owned. | Continuity-on plans now receive a neutral per-scene camera chassis, which activates the ordinary world/runtime seam without inventing movement; blocking still owns x/y/zoom. Ripple placement inverts its actual transformed positioning parent. Single-shell children and same-entity lockup headline/buttons receive contextual framing, primary dwell is never reclaimed, and app-window `open` lowers to `rows`. Exact replay raises headline/list/button landings into range, removes the anchor/unsettled failures and final CTA crop, and cuts penalty 83→48. Remaining rejected-source layout/morph and authored payoff-timing findings are genuine authored/WS-D2 work, not host contradictions. | Host attempt cause and visible camera bugs fixed; residual authored classes documented |
